# Web framework
flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1

# Image handling
Pillow==10.4.0
pillow-heif==0.18.0
python-magic==0.4.27

# Email
resend==2.4.0

# Auth (signed tokens voor QR + magic-link, werkzeug voor admin password-hash)
# werkzeug komt al mee als Flask-dep, expliciet listen voor de zekerheid
itsdangerous>=2.2.0
werkzeug>=3.0
cryptography>=42.0.0

# Scheduled tasks (trash auto-purge, etc.)
APScheduler==3.10.4

# QR code generation
qrcode[pil]==7.4.2

# PDF generation (admin QR-poster voor sluiswachters)
reportlab==4.2.5

# HTTP / utilities
requests==2.32.3
